git add -all 

git commit -m "<message>"

git push (to the origin)

git remote -v 

git push --set-upstream (to a particular branch)

git switch -c (new Branch)

git fetch (download objects, ref from another repo) <add in the repo link>

git pull (pull request for a forked repo/compare)

git checkout (checkout which branch your in)

git status

Git merge (not used much honestly)

Git log (Log events **important command)

Git branch

git checkout <dir/repo>